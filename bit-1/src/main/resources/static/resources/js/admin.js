"use strict"
var admin = admin || {}
admin = (() => {                         //( ()=>{} )(  )
	let init = () => {
		onCreate()
	}
	let onCreate = () => { // 기능
		setContentView()
	}
	let setContentView = () => {   //속성
	}
	return {init}
	
})()


